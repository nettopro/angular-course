//Can use type instead of interface quite interchangeably
//Look it up for better explanations as to why you would 
//For objects its more common using interfaces 
export interface UserProfile { 
    id: string;
    avatar: string;
    name: string;
}